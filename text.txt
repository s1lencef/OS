main() {
вывод на экран pid и ppid;
pid_t	pid	=	fork();
if (pid == 0) {
exec(“prog”,...);
}else if (pid > 0) {
while (waitpid(...) == 0) {
вывод на экран слова «ждем»;
sleep(1);
}
вывод на экран код_завершения дочернего процесса;
};
